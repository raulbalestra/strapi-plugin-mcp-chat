// Este arquivo é gerado automaticamente pelo plugin do TanStack Router/Start
// ao rodar `vite dev` ou `vite build`. O stub abaixo existe apenas para o projeto
// ser tipável antes da primeira execução; ele é sobrescrito assim que o dev server
// sobe. NÃO edite manualmente.
import { createRootRoute } from '@tanstack/react-router'

const rootRoute = createRootRoute()

export const routeTree = rootRoute
